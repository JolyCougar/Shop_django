# from django.urls import path
# from .views import AddReviewView

# app_name = 'review'

urlpatterns = [
    # path('product/<int:product_id>/add_review/', AddReviewView.as_view(), name='add_review'),

]
